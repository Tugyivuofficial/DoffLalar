'use client'
import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { createClient } from '@/lib/supabase-browser'
export default function AuthPage(){ const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState(''),[loading,setLoading]=useState(false); const router=useRouter(); const supabase=createClient(); async function signIn(){ setLoading(true); setMsg(''); const {error}=await supabase.auth.signInWithPassword({email,password}); setLoading(false); if(error){setMsg(error.message);return} router.replace('/home') } async function signUp(){ setLoading(true); setMsg(''); const {error}=await supabase.auth.signUp({email,password}); setLoading(false); if(error){setMsg(error.message);return} setMsg('Check email, then login.') } async function google(){ await supabase.auth.signInWithOAuth({provider:'google',options:{redirectTo:`${location.origin}/home`}}) } return <main className="min-h-screen bg-bg px-5 py-10 max-w-md mx-auto flex flex-col justify-center"><p className="mono text-red">COMPETITIVE MOBILE MATCHES</p><h1 className="text-5xl font-black leading-none">STANDOFF 2<br/>MONGOLIA</h1><div className="card p-4 mt-8 space-y-3"><input className="input" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/><button disabled={loading} onClick={signIn} className="btn redbtn w-full">LOGIN</button><button disabled={loading} onClick={signUp} className="btn ghost w-full">CREATE ACCOUNT</button><button onClick={google} className="btn bg-white text-black w-full">CONTINUE WITH GOOGLE</button>{msg&&<p className="text-sm text-red">{msg}</p>}</div></main> }
